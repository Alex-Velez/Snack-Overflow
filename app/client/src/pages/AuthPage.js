import React from 'react';

export default function AuthPage() {
    return (
        <div>
            <h1>Log In / Sign Up</h1>
            {/* TODO: replace with log in / sign up UI */}
        </div>
    );
}
